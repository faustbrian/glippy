# v0.6 restic Adjudication, 2026-08-24

## Evidence

Pinned corpus run `32698988231` used Glippy revision
`7840a0c03cf0d58ca0994ebd52e8d5b934ceeabf` and restic revision
`a80be1478a4c537f8396e0db2b05120aa78f11e0`. All four profiles completed:

| Profile | Findings |
| --- | ---: |
| default | 1 |
| recommended | 12 |
| strict | 201 |
| pedantic | 240 |

Manual review classifies 11 findings as actionable and one as intentional
testing noise.

The actionable findings are all `unchecked-scanner-error` diagnostics:

- six test helpers can accept partial command, fixture, or subprocess output
  and either false-pass or obscure the actual scan failure;
- the rclone, SFTP, and generic command stderr goroutines can stop draining a
  subprocess pipe after a scan failure, hiding diagnostics and potentially
  stalling a writer;
- the release helper checks `Scanner.Err` inside the scan loop, where no
  terminal scan error is available, and never checks it after the loop; and
- self-update checksum lookup converts a scan failure into a generic missing
  hash error.

The sole default `unreachable-code` finding is a final bare return immediately
after `testing.T.Fatalf` in `internal/archiver/archiver_test.go`. It is an
intentional termination shim and is covered by the precision correction in
Glippy commit `972cfcf`.

The run completed across restic's backup, cgo, and CLI selection without a
Glippy crash, invalid machine output, unsupported source state, or incomplete
profile.

## Comparator Coverage

`go vet` completed without findings and does not report the scanner paths.
Staticcheck reported two API deprecations, eight error-string style findings,
and one unused field. The deprecated cryptographic packages require explicit
interoperability and migration decisions; the remaining findings are style or
dead-code policy. None establishes a missing default Glippy rule from this
restic run.

## Decision

Retain all 11 scanner diagnostics. Suppress only the exact final testing return
through the already admitted testing-termination shim contract. restic requires
no further precision correction or default-rule candidate from this run.

The complete exact corpus must rerun at the latest Glippy revision before this
adjudication contributes to the v0.6 exit evidence.
