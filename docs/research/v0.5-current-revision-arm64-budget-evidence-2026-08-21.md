# v0.5 Current-Revision arm64 Budget Evidence, 2026-08-21

## Decision

Glippy revision `835a2961d4e47c43e21c551ac56313a743f98667` passes the
existing release budgets on native Darwin arm64 and Docker-hosted Linux arm64
with the pinned Go 1.26.5 toolchain and immutable formatter and typed corpora.

The campaign also proves that the release workflow's Darwin typed diagnostic
fingerprint was stale after the v0.5 catalog and semantic-analysis expansion.
The former `030f4474...c7282d0` value rejects the current output. Five complete
Darwin samples and three additional normalized invocations agree on replacement
SHA-256 `13f9c3dd006a105196c13f766a1c849a882754b3083979e9386c78cc2fdb53d2`.

This updates the workflow guard; it does not replace the required four-native-
runner campaign for the eventual release candidate.

## Environment And Workloads

- Darwin: macOS 27.0 arm64 on an Apple M4 Max with 128 GiB RAM.
- Linux: Linux 7.0.12 arm64 through Docker Desktop 29.7.2 on the same physical
  Darwin host.
- Linux image: `golang:1.26.5-bookworm` at
  `sha256:1ecb7edf62a0408027bd5729dfd6b1b8766e578e8df93995b225dfd0944eb651`.
- Toolchain: Go 1.26.5 on both operating systems.
- Formatter corpus: `faustbrian/golib` at
  `f28f85133ac6d13169745807fc39e2d5ef6bf780`.
- Typed corpus: `sqlc-dev/sqlc` at
  `8a7cddfbb9088666eb981645285d7699e71dcb54`.
- Editor limit: 250 ms for every fresh process.
- Formatter limits: 90 seconds and 2,147,483,648 bytes for every sample.
- Typed limits: 40 seconds and 2,147,483,648 bytes for every sample.
- Samples: 20 editor invocations plus five formatter and five typed runs per
  operating system.

The repository and both corpus checkouts were clean and revision-bound before
measurement. Every build and module cache was task-owned. The Linux container
was removed after the run; the pinned image existed before this task and was
left unchanged.

## Results

| Runtime | Workload | Elapsed range | Peak RSS range | Result |
| --- | --- | ---: | ---: | --- |
| Darwin arm64 | editor | 7.476-8.341 ms | not sampled | pass |
| Darwin arm64 | formatter | 4.580-5.520 s | 1,347,600,384-1,478,885,376 B | pass |
| Darwin arm64 | typed | 1.650-22.550 s | 510,803,968-1,360,035,840 B | pass |
| Linux arm64, Docker | editor | 2.938-3.772 ms | not sampled | pass |
| Linux arm64, Docker | formatter | 9.570-11.230 s | 1,020,600,320-1,317,863,424 B | pass |
| Linux arm64, Docker | typed | 1.780-23.390 s | 522,604,544-1,256,058,880 B | pass |

Darwin RSS uses `/usr/bin/time -l`; Linux RSS uses GNU `/usr/bin/time -v`
inside the container. Both represent the measured Glippy process rather than
aggregate host or container memory. The Docker numbers are runtime evidence on
Linux arm64, not independent-host or native GitHub-runner evidence.

The complete typed policy produced 353 deterministic findings on Darwin arm64:

| Rule | Findings |
| --- | ---: |
| `discarded-error` | 343 |
| `nil-error-wrap` | 1 |
| `resource-not-closed` | 5 |
| `unchecked-scanner-error` | 3 |
| `unchecked-writer-error` | 1 |

The command outcome was the expected findings exit category with no package or
source errors. The fingerprint changed because the current correctness and
suspicious catalog and its interprocedural analyses are materially broader than
the revision that established the older digest. The replacement is not a
suppression or weakened finding set.

## Red And Green Gate

With the old workflow value, the current Darwin campaign stopped with:

```text
typed-lint diagnostic fingerprint changed:
13f9c3dd006a105196c13f766a1c849a882754b3083979e9386c78cc2fdb53d2 !=
030f4474aec74877307118376e77e8ad7254a46c32777242fd11e3223c7282d0
```

After supplying the replacement fingerprint, all five typed samples completed
within budget. Three additional normalized typed invocations independently
produced the same replacement digest.

## Remaining Boundary

This campaign closes the local supported-OS arm64 rehearsal for the current
binary sources. It does not provide Darwin amd64 or Linux amd64 evidence, does
not run on independent physical hosts, and does not prove that the eventual
public source revision is byte-identical to this local tree. After canonical
repository synchronization, the exact pushed revision must run the manual
four-native-runner workflow and retain its raw artifacts before release review.
