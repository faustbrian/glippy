# Glippy v0.2 Rule Catalog Evidence, 2026-08-13

## Supported Rules And Presets

The canonical metadata and generated `docs/lint-rules.md` remain the behavioral
authority. This matrix records the v0.2 catalog after the bug-catching and
initial pedantic expansion.

| Rule | Preset | Tier |
| --- | --- | --- |
| `almost-swapped` | suspicious | types |
| `append-no-values` | correctness | types |
| `atomic-update-assignment` | correctness | types |
| `bad-bit-mask` | correctness | types |
| `context-cancel-leak` | correctness | control flow |
| `context-key` | suspicious | types |
| `contradictory-condition` | correctness | types |
| `copied-lock` | correctness | types |
| `defer-in-infinite-loop` | suspicious | control flow |
| `defer-in-loop` | suspicious | syntax |
| `deferred-time-since` | correctness | types |
| `discarded-error` | suspicious | types |
| `duplicate-condition` | correctness | syntax |
| `errors-as-target` | correctness | types |
| `errors-is-arguments` | suspicious | types |
| `http-canonical-header-key` | suspicious | types |
| `http-response-before-error` | correctness | types |
| `identical-branches` | suspicious | syntax |
| `invalid-slog-arguments` | correctness | types |
| `invalid-struct-tag` | correctness | types |
| `invalid-unmarshal-target` | correctness | types |
| `impossible-comparison` | correctness | types |
| `impossible-type-assertion` | correctness | types |
| `inconsistent-receiver-name` | pedantic | types |
| `ineffective-break` | correctness | syntax |
| `lock-held-across-blocking-call` | suspicious | types |
| `loop-capture` | correctness | types |
| `mixed-receiver-type` | pedantic | types |
| `nil-context` | correctness | types |
| `nil-function-comparison` | correctness | types |
| `nilness` | suspicious | SSA |
| `oversized-shift` | correctness | types |
| `printf-arguments` | correctness | types |
| `redundant-bool-comparison` | style | types |
| `redundant-else` | pedantic | syntax |
| `resource-not-closed` | suspicious | types |
| `self-assignment` | correctness | types |
| `standard-method-signature` | correctness | types |
| `subsumed-condition` | suspicious | types |
| `suspicious-string-conversion` | suspicious | types |
| `suspicious-range` | suspicious | types |
| `testing-goroutine-call` | correctness | types |
| `time-duration-unit` | suspicious | types |
| `time-layout` | correctness | types |
| `unnecessary-conversion` | pedantic | types |
| `unnecessary-sprintf` | pedantic | types |
| `unreachable-code` | correctness | types |
| `unsafe-host-port` | correctness | types |
| `unused-result` | correctness | types |
| `waitgroup-misuse` | correctness | types |

The `performance`, `complexity`, `restriction`, and `migration` contracts exist
but have no built-in rule in this catalog. The groups remain selectable only as
documented: restriction is never enabled wholesale and migration requires an
explicit target.

## Requested Bug-Catching Coverage

All sixteen requested capability areas have an admitted v0.2 rule or documented
standard-library family audit: identical branches, self assignment, almost
swapped assignments, impossible comparisons, bad bit masks, contradictory and
subsumed conditions, defer in loops, resource ownership, discarded errors,
copied locks, loop capture, impossible type assertions, suspicious range
mutation, locks held across blocking calls, and context/errors/time/HTTP/atomic
API misuse.

The expanded standard-library audit admits `context-cancel-leak`, `nil-context`,
`errors-as-target`, `time-duration-unit`, `time-layout`,
`http-response-before-error`, `http-canonical-header-key`, and
`atomic-update-assignment` alongside the earlier context and errors rules. It
rejects obsolete supported-version timer advice and defers target-specific or
ownership contracts that lack sufficient precision.

## Standard Analyzer Interoperability

| Glippy rule | Go analyzer | Product-owned additions |
| --- | --- | --- |
| `self-assignment` | `assign` | Stable identity, policy, suppression, baseline, reporting, generated-file behavior |
| `copied-lock` | `copylocks` | Stable identity, deterministic scheduling and reporting, policy integration |
| `impossible-type-assertion` | `ifaceassert` | Stable identity, exact source mapping, policy integration |
| `http-response-before-error` | `httpresponse` | Stable identity, policy integration, generated and type-error boundaries |
| `atomic-update-assignment` | `atomic` | Stable identity, policy integration, deterministic diagnostics |
| `contradictory-condition` | filtered `bools` | Contradictory subset only, stable identity, cohesive policy |
| `errors-as-target` | `errorsas` | Stable identity, policy integration, source mapping |
| `time-layout` | `timeformat` | Stable identity and suggestion-only fix classification |
| `append-no-values` | `appends` | Stable identity, policy integration, deterministic diagnostics |
| `deferred-time-since` | `defers` | Stable identity and explicit no-fix boundary |
| `invalid-slog-arguments` | `slog` | Stable identity and structured-argument contract |
| `invalid-struct-tag` | `structtag` | Stable identity and run-despite-errors policy |
| `invalid-unmarshal-target` | `unmarshal` | Stable identity and exact point range |
| `nil-function-comparison` | `nilfunc` | Stable identity and typed object contract |
| `oversized-shift` | `shift` | Stable identity and upstream dead-code exclusions |
| `printf-arguments` | `printf` | Stable identity, wrapper facts, stripped global flags, suggestion mapping |
| `standard-method-signature` | `stdmethods` | Stable identity and canonical signature catalog |
| `suspicious-string-conversion` | `stringintconv` | Suspicious placement and two explicit suggestion alternatives |
| `testing-goroutine-call` | `testinggoroutine` | Stable identity with experimental subtest mode disabled |
| `unreachable-code` | `unreachable` | Run-despite-errors policy and suggestion-only removal |
| `unsafe-host-port` | `hostport` | Shared type-index prerequisite and suggestion-only replacement |
| `unused-result` | `unusedresult` | Authoritative defaults with package-global flags stripped |
| `waitgroup-misuse` | `waitgroup` | Stable identity and concurrency-safety category |

`loop-capture` and `context-cancel-leak` remain native because their current
source-version and diagnostic contracts differ from directly exposing
`loopclosure` and `lostcancel`. The admission records document those decisions.

The complete supported, partial, native-overlap, and unsupported default-vet
matrix is documented in `docs/go-vet-compatibility.md`. Glippy does not claim
complete `go vet` replacement.

## Go Vet Compatibility Pack Evidence

The fifteen-rule 2026-08-14 expansion has exact positive and nearby-negative
fixtures, generated-file and type-error policy tests, suppression and
source-version gates, deterministic JSON/baseline/explain coverage, and
suggestion mapping for every upstream fix. Unambiguous `printf`, `unreachable`,
and `hostport` suggestions additionally pass formatted, validated, repeated CLI
application. Each rule has a dated admission record under `docs/research`.

One-iteration typed package probes on Go 1.26.6 darwin/arm64 ranged from
99,869,417 ns (`append-no-values`) to 587,996,708 ns (`printf-arguments`). These
include package loading and are proportional admission evidence rather than a
stable latency budget. Non-mutating dogfood enabled only these fifteen rules and
found no diagnostics across 178 Glippy files or 57 files in
`go-libraries/pkg/prompts` at
`2c9842015ab62fd7790f0d99bf54855ffa7000f2`; the target's pre-existing wider
repository dirt was not modified.

## Initial Pedantic Evidence

The initial pedantic group contains five individually configurable, warning
level, generated-file-excluding rules with no fixes:

| Rule | Median time | Median bytes | Median allocations |
| --- | ---: | ---: | ---: |
| `redundant-else` | 7,858 ns/op | 11,056 B/op | 84 allocs/op |
| `unnecessary-conversion` | 25,578,375 ns/op | 174,993 B/op | 1,152 allocs/op |
| `unnecessary-sprintf` | 72,726,575 ns/op | 1,665,131 B/op | 13,229 allocs/op |
| `inconsistent-receiver-name` | 23,964,742 ns/op | 199,560 B/op | 1,418 allocs/op |
| `mixed-receiver-type` | 24,618,450 ns/op | 199,472 B/op | 1,417 allocs/op |

The syntax benchmark measures an already-loaded source file. Types benchmarks
include a complete one-package load, which dominates the observed values.

Non-mutating pedantic dogfood found two true-positive `redundant-else`
opportunities in Glippy, one true-positive `redundant-else` opportunity in
`go-libraries/pkg/prompts`, and one intentional `mixed-receiver-type` exception
in that external package. The immutable prompts revision remained
`6ed3a06a4e1aba412d2a6b91454774234f30a464`, including its pre-existing dirty
state.

## Deferred Pedantic Candidates

The initial audit defers broad allocation, interface-breadth,
signature-complexity, general string-construction, documentation, nested-flow,
and closure rules until their precision and configuration contracts have
real-code evidence. Blank-identifier simplification is a bounded next candidate;
`discarded-error` already owns the bug-prone error case. See the full pedantic
catalog audit for individual dispositions.
