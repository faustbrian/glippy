# v0.6 Unsupported Source Boundaries, 2026-08-24

## Evidence

Pinned corpus run `32698988231` used Glippy revision `7840a0c` for two
repositories outside the accepted source-language range:

| Repository | Revision | Selected Go version | Profile outcome |
| --- | --- | ---: | --- |
| Go-Ethereum | `02b73d4ea7181464175e0a6cbecc0a3a2655a562` | 1.24 | source error, exit 2 |
| go-sqlite3 | `58c8e145308ceded07d1df2ac1b65999499e7055` | 1.21 | source error, exit 2 |

Default, recommended, strict, and pedantic each returned zero diagnostics and
`complete: false`. The versioned JSON report identifies the repository
`go.mod` and states that Glippy supports Go 1.25 through Go 1.26. This is the
documented source-version refusal path, not a clean analysis result.

The analysis preflight and external comparators ran independently. Their output
does not make the Glippy profiles complete and must not be substituted for the
missing Glippy coverage.

## Decision

Retain the supported-source policy. Record both repositories as explicit
unsupported-construct gaps in canonical adjudication. Do not rewrite their
`go` directives or use derivative source shims merely to make the corpus look
complete. Revisit the repositories when Glippy's supported source range expands
or when the pinned corpus selects revisions inside that range.
