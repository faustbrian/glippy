# v0.6 CockroachDB Build Prerequisite, 2026-08-24

## Evidence

Pinned corpus run `32698988231` used Glippy revision `7840a0c` and CockroachDB
revision `8812064a015d2faf99d3fc7e15880f94042954b0`. Analysis preflight failed
before Glippy, Go vet, or Staticcheck could analyze the repository:

```text
pkg/internal/team/team.go:82:12: pattern TEAMS.yaml: no matching files found
```

The package contains both a generator directive that copies the repository
root `TEAMS.yaml` and a `go:embed TEAMS.yaml` directive. The package-local
generated file is absent from the clean pinned checkout. Consequently, all four
Glippy profiles and both comparators are incomplete and have no findings that
can be adjudicated.

## Decision

Record an unsupported-build-prerequisite gap. Do not mutate the immutable
external checkout or silently run generators. A future corpus revision may use
a separately documented, digest-bound overlay if the corpus contract first
defines derivative-source provenance and proves that the overlay cannot hide
other checkout changes.
