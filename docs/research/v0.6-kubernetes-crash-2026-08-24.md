# v0.6 Kubernetes Crash And Correction, 2026-08-24

## Evidence

Pinned corpus run `32698988231` used Glippy revision `7840a0c` and Kubernetes
revision `e81f39c0e03ce8ed8e2660c9147b391edd9e262b`. Analysis preflight and Go vet
completed, but every Glippy profile panicked before producing diagnostics. The
retained stack trace identifies the first failing boundary:

```text
typeutil.StaticCallee
internal/analysis.(*noReturnAnalysis).buildTestingSkip
```

Testing-skip inference inspected a source-defined no-return function whose live
terminal CFG block was not a call. `terminalCall` correctly returned nil, but
the result was passed directly to `typeutil.StaticCallee`, which dereferenced
it. The crash is common to default, recommended, strict, and pedantic because
the affected control-flow rule is selected in every profile.

## Correction

Commit `7a7f720` centralizes a conservative static-callee lookup that rejects a
nil call or incomplete type maps before invoking `typeutil.StaticCallee`. A
regression fixture using `func stop() { select {} }` proves that the no-return
function is not misclassified as a testing skip and no longer panics. Focused
and complete control-flow analysis tests, the analysis package suite, vet, and
independent review passed for that correction.

## Decision

Record this run as a crash gap and retain all four Glippy profiles as incomplete
evidence. The source correction is complete, but Kubernetes must rerun in the
isolated corpus workflow at the latest candidate before the crash gap can be
closed or any findings can be adjudicated. Do not reproduce the full
Kubernetes workload locally.
