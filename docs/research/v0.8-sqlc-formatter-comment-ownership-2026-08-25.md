# v0.8 SQLC Formatter Comment Ownership, 2026-08-25

## Evidence

SQLC-only corpus run
[`32807031805`](https://github.com/faustbrian/glippy/actions/runs/32807031805)
used Glippy revision `1a3acabfdb709bbe9c641e7ab6781f24292c212d` and SQLC
revision `99a7d7d0c7b913ded6a60a9d038ece99dcdf7892`. All lint profiles and
the non-mutating fix preview completed. Default reported zero diagnostics;
recommended reported the same three actionable `unchecked-scanner-error`
diagnostics recorded in v0.6.

Formatter check selected 619 files but returned internal-error exit 6 with
`comment 3 has no proven output owner`. The normalized `format.json` artifact
has SHA-256 `bdd3454432c63a27d2148e5b41e5d4c401852fa5424af668782c00fbe26adb8c`.
No external repository file was modified.

## First Divergence

An exact per-file reproduction isolated
`internal/sql/ast/type_name.go`. Its `addMods:` label is followed by the line
comment describing type modifiers and then an `if` statement. The label lowerer
lowered the nested statement but never claimed comments between the label colon
and that statement's physical start. Final comment accounting therefore
correctly rejected the output rather than losing or moving the comment.

The formatter now claims that exact physical gap and emits its comments inside
the label's indented body before the nested statement. It does not weaken final
comment accounting or introduce a SQLC-specific exception.

Review of the complete label boundary also found that a label ending a block
uses an implicit empty statement. Comments between that label and the closing
brace previously bypassed the same ownership rule. The shared label lowerer now
claims those comments as an indented label body while preserving comment order;
labels without comments retain their existing output.

Same-line label comments remain line suffixes. This is required for suppression
directives such as `//nolint`, whose physical-line token ownership is semantic
input to external analyzers. Focused nested-statement and implicit-empty label
cases prove that the directive anchor remains stable. Physical blank gaps are
also retained between a label suffix, later label-body comments, the nested
statement, and an implicit closing boundary.

## Verification Boundary

The focused regression failed before the implementation with the same
ownerless-comment error and passed afterward. The complete `internal/format`
package passed. A separate focused regression for a comment after a
block-closing label also failed with the ownerless-comment error before the
extension and passes afterward. Independent review then found same-line label
suppression directives failed equivalence after being moved; focused cases for
both label forms failed before line-suffix preservation and now pass.
Re-review found that mixed suffix/body comment gaps and a trailing implicit-body
directive gap were collapsed; three focused cases failed before physical-gap
preservation and now pass. A final adjacent review found suffix-only blank gaps
before nested and implicit-empty statements; both focused cases failed before
the same boundary-preservation rule was completed and now pass.
A compatibility review then found that the generalized rule retained blank
lines on labels without comments. Gap preservation is now limited to
comment-bearing boundaries, and focused nested and implicit-empty cases retain
the established canonical spacing.
Formatting the exact SQLC file then preserved the comment, reparsed, and
reached an idempotent fixed point. A sequential per-file scan of the exact SQLC
checkout reported no remaining internal formatter error.

The complete isolated corpus workflow must rerun at the pushed correction to
prove aggregate formatter completeness, retained artifact reconciliation, and
fix-preview behavior. No repository-wide local formatter run or prohibited
runtime, process-control, RSS, interruption, or Docker-impacting probe was run.
