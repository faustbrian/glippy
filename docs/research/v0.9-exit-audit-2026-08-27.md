# v0.9 Stability Freeze Exit Audit, 2026-08-27

## Decision

Status: pending the final audit-bearing candidate rerun and final-diff review.

Candidate `8f52f20deea6ceb9bb316d312e26e65646ca9f6d` has passed CI,
the final fuzz matrix, all four native release-budget jobs, and cross-runner
release reproducibility. Pinned corpus run `33040254261` passed all 20 jobs and
its exact adjudication retains 334 classified findings, zero default or
recommended false positives, and the same 11 accepted gaps. The release
contract and evidence diff must still be committed and pass exact CI, native
candidate reproduction, and final review. Stable-v1 roadmap progress therefore
remains 85%; no tag or public release is authorized by this audit until every
pending row below is complete.

## Gate

| v0.9 requirement | Exact evidence | Result |
| --- | --- | --- |
| Frozen formatter, rule, profile, configuration, CLI, completion, reporter, exit-code, and machine contracts | Release-budget run `33040254351` reproduced the v1 contracts on macOS and Linux amd64/arm64 | Complete |
| Zero known default false positives | Corpus run `33040254261` passed all 17 audits; every one of its 334 finding fingerprints matches the prior human adjudication and none is a false positive | Complete |
| No release-blocking latency or memory regression | Release-budget run `33040254351` passed every native latency and aggregate-memory ceiling | Complete |
| Current product-reference comparison | `docs/research/v0.9-reference-refresh-2026-08-25.md` records Oxfmt, Oxlint, Oxc, Clippy, rustfmt, gofmt, vet, and Staticcheck boundaries | Complete |
| Exact CI, race, integration, and fix safety | CI run `33040222753` passed the repository-owned verification gate | Complete |
| Final bounded fuzzing | Fuzz run `33040254402` passed source, formatter, document renderer, fix coordinator, suppressions, configuration, baseline, cache, and analysis-fact jobs | Complete |
| Native tagless candidates | Release-budget run `33040254351` retained candidates for Darwin and Linux on amd64 and arm64 | Complete |
| Cross-runner reproducibility | The dependent job in run `33040254351` proved release files and frozen contract snapshots byte-identical across all four runners | Complete |
| Historical upgrade rehearsal | Every native job in run `33040254351` accepted the retained Gox v0.1.0 and early-Glippy configuration and baseline fixtures | Complete |
| Exact pinned corpus and combined evidence | Corpus run `33040254261` passed harness validation, matrix resolution, all 17 repository audits, and combined collection under one run identity; the canonical v1 adjudication and report validate 11 accepted gaps covering 19 incomplete units | Complete |
| Final audit-bearing candidate | Commit the release contract and corpus evidence, then pass exact CI and native release-budget/reproducibility gates at that revision | Pending |
| Final independent review | Review must cover the complete final documentation diff and exact acceptance evidence | Pending |

## Scope

The supported release targets remain macOS and Linux on amd64 and arm64.
Windows, network, distributed, and userspace filesystems, and forced-power-loss
durability remain outside the v1 write/fix contract. Public corpus repositories
are immutable validation inputs; no upstream adoption, custom Action, editor
plugin, package-manager integration, hosted service, or contract marketplace is
part of the v1 gate.

No tag or release has been created.
