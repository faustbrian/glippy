# v0.9 Stability Freeze Exit Audit, 2026-08-27

## Decision

Status: complete.

Source-bearing candidate `18004f2eba702471e03c9ab5656c905cf470e946`
passed exact CI `33056719958` and native release-budget/reproducibility run
`33056742540`. Final fuzz run `33040254402` and pinned corpus run `33040254261`
carry forward from `8f52f20` because the intervening diff changes only release
documentation, evidence, workflow note selection, and the maintainer-only rule
catalog description. The corpus adjudication retains 334 classified findings,
zero default or recommended false positives, and the same 11 accepted gaps.
Independent final review found no issues. v0.9 is complete and stable-v1
roadmap progress advances from 85% to 95%.

## Gate

| v0.9 requirement | Exact evidence | Result |
| --- | --- | --- |
| Frozen formatter, rule, profile, configuration, CLI, completion, reporter, exit-code, and machine contracts | Release-budget run `33056742540` reproduced the v1 contracts on macOS and Linux amd64/arm64 at `18004f2` | Complete |
| Zero known default false positives | Corpus run `33040254261` passed all 17 audits; every one of its 334 finding fingerprints matches the prior human adjudication and none is a false positive | Complete |
| No release-blocking latency or memory regression | Release-budget run `33056742540` passed every native latency and aggregate-memory ceiling | Complete |
| Current product-reference comparison | `docs/research/v0.9-reference-refresh-2026-08-25.md` records Oxfmt, Oxlint, Oxc, Clippy, rustfmt, gofmt, vet, and Staticcheck boundaries | Complete |
| Exact CI, race, integration, and fix safety | CI run `33056719958` passed the repository-owned verification gate at `18004f2` | Complete |
| Final bounded fuzzing | Fuzz run `33040254402` passed source, formatter, document renderer, fix coordinator, suppressions, configuration, baseline, cache, and analysis-fact jobs | Complete |
| Native tagless candidates | Release-budget run `33056742540` retained candidates for Darwin and Linux on amd64 and arm64 | Complete |
| Cross-runner reproducibility | The dependent job in run `33056742540` proved release files and frozen contract snapshots byte-identical across all four runners | Complete |
| Historical upgrade rehearsal | Every native job in run `33056742540` accepted the retained Gox v0.1.0 and early-Glippy configuration and baseline fixtures | Complete |
| Exact pinned corpus and combined evidence | Corpus run `33040254261` passed harness validation, matrix resolution, all 17 repository audits, and combined collection under one run identity; the canonical v1 adjudication and report validate 11 accepted gaps covering 19 incomplete units | Complete |
| Final audit-bearing candidate | Revision `18004f2` passed exact CI `33056719958` and native release-budget/reproducibility run `33056742540` | Complete |
| Final independent review | GPT-5.6 Sol medium review covered the complete release-preparation diff and exact acceptance evidence and reported no findings | Complete |

## Scope

The supported release targets remain macOS and Linux on amd64 and arm64.
Windows, network, distributed, and userspace filesystems, and forced-power-loss
durability remain outside the v1 write/fix contract. Public corpus repositories
are immutable validation inputs; no upstream adoption, custom Action, editor
plugin, package-manager integration, hosted service, or contract marketplace is
part of the v1 gate.

The authorized `v1.0.0` publication transaction remains the v1 boundary. No tag
or release has been created by this audit.
