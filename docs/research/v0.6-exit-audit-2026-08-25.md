# v0.6 Exit Audit, 2026-08-25

## Decision

The v0.6 real-world rule-validation milestone is complete at analyzed Glippy
revision `ee2ea4fccced05673d095fc08e841c0f44f6fc07`. Exact pinned corpus run
[`32723865179`, attempt 2](https://github.com/faustbrian/glippy/actions/runs/32723865179/attempts/2)
passed all 20 jobs and produced one coherent review artifact for 17 public Go
repositories. Stable-v1 roadmap progress advances from 55% to 65%, and the
active milestone becomes v0.7 semantic depth and credible fixes.

This audit does not declare Glippy v1-ready. It does not authorize a tag or
release.

## Requirement Audit

| v0.6 requirement | Authoritative evidence | Result |
| --- | --- | --- |
| Pinned substantial corpus | [`benchmarks/corpus/manifest.json`](../../benchmarks/corpus/manifest.json) records 17 exact revisions spanning large repositories, workspaces, generators, cgo, databases, servers, CLIs, and libraries | Complete |
| Run default, recommended, strict, and pedantic | Exact attempt 2 completed all repository jobs; the canonical report binds statistics for every profile and records incomplete coverage rather than treating refused source or build states as clean | Complete with four documented unsupported boundaries |
| Manually classify every default and recommended finding | [`v0.6-corpus-adjudication-32723865179.json`](v0.6-corpus-adjudication-32723865179.json) classifies all 386 entries; none remains `unresolved` | Complete |
| Compare with vet and Staticcheck | Every analyzable repository has bound comparator output; CockroachDB's two incomplete comparators are tied to its preflight build prerequisite | Complete with one documented build boundary |
| Record false positives, missed defects, crashes, unsupported constructs, latency, and memory | The aggregate summary records 59 false-positive entries, five missed-defect gaps, closure of the Kubernetes crash, four unsupported boundaries, profile durations, allocated bytes, and allocation counts | Complete; peak RSS remains a later milestone |
| Convert demonstrated gaps into a rule queue | The generated report deterministically queues `ineffective-assignment` and `exact-suffix-as-cutset` from five concrete comparator gaps | Complete |
| Add a nursery group | Nursery is an explicit unstable opt-in preset, excluded from curated profiles, documented in the CLI, configuration, compatibility policy, rule roadmap, and generated catalog; noisy `resource-used-after-close` evidence was moved there during v0.6 | Complete |

## Signal Result

The final classified profile entries are:

| Profile | True positive | False positive | Comparator duplicate | Total |
| --- | ---: | ---: | ---: | ---: |
| default | 66 | 26 | 16 | 108 |
| recommended | 229 | 33 | 16 | 278 |

The 26 known default false-positive entries are not hidden or described as v1
quality. They are explicit precision work for v0.7 and must be resolved before
the v0.9 zero-known-default-false-positive freeze. The 33 recommended entries
include repeated default findings and additional recommended-rule precision
boundaries.

Exact reconciliation against the previous canonical run carried 159 unique
fingerprints, removed 27 unique superseded fingerprints, and added 119 unique
Kubernetes fingerprints. All added fingerprints were manually adjudicated.
The final candidate removed 54 repeated profile entries demonstrated as noise
without losing retained actionable findings in the other repositories.

## Accepted Boundaries

Ten incomplete profile or comparator units remain explicit across
CockroachDB, Go-Ethereum, go-sqlite3, and Moby. They represent generated build
input, accepted source-version policy, and cgo typed-coverage limitations.
They are recorded corpus outcomes rather than unclassified diagnostics.

The aggregate resource measurements sum per-repository executions. They do
not prove stable macOS/Linux cold, warm, editor, or peak-RSS budgets. Those are
v0.8 requirements. Network filesystems, forced power-loss durability, and
runtime process-control probes also remain outside this milestone and were not
executed locally.

## Exit Result

All seven v0.6 requirements have current exact-revision evidence. The
milestone closes at 65% stable-v1 progress. v0.7 should use the corpus-backed
false positives and two-rule queue to prioritize bounded interprocedural
precision, lifecycle and error-flow depth, credible fixes, and interaction
testing. No public corpus repository was modified, and no tag or release was
created.
