# v0.6 etcd Adjudication, 2026-08-24

## Evidence

Pinned corpus run `32698988231` used Glippy revision
`7840a0c03cf0d58ca0994ebd52e8d5b934ceeabf` and etcd revision
`c34dc7ee0048fd2bcc44d50beff002e5e8069b69`. All four profiles completed:

| Profile | Findings |
| --- | ---: |
| default | 0 |
| recommended | 0 |
| strict | 40 |
| pedantic | 41 |

There are no default or recommended findings to classify. Strict reports 34
discarded errors, three resource-lifetime findings, two excessive-nesting
findings, and one loop defer. Pedantic adds one redundant closure. These remain
opt-in diagnostics and do not affect the v0.6 default/recommended precision
gate.

The run completed across etcd's workspace and generated-source selection
without a Glippy crash, invalid machine output, unsupported source state, or
incomplete profile.

## Comparator Coverage

`go vet` completed without findings. Staticcheck reported one `SA1019` use of
the deprecated `github.com/golang/protobuf/proto` package in the benchmark
tool. The exact import carries `//nolint:staticcheck`, so the repository has
explicitly accepted that migration debt. A third-party API migration belongs
to a targeted migration policy rather than the default correctness profile and
does not establish a missing default Glippy rule.

## Decision

Record etcd as clean for the default and recommended profiles. It adds no
precision correction or default-rule candidate. Retain the strict and pedantic
findings as opt-in output for later profile-level usefulness review.

The complete exact corpus must rerun at the latest Glippy revision before this
adjudication contributes to the v0.6 exit evidence.
