# v0.6 Prometheus Adjudication, 2026-08-24

## Evidence

Pinned corpus run `32698988231` used Glippy revision `7840a0c` and Prometheus
revision `d15adb9ad7e5d9fbde3a9a8f30200593a5a14d86`. All referenced source
digests match the retained immutable source snapshot. All four profiles
completed:

| Profile | Findings |
| --- | ---: |
| default | 6 |
| recommended | 10 |
| strict | 664 |
| pedantic | 874 |

The run analyzed 676 owned files in 205 packages and loaded 765 files. Manual
review classifies six default or recommended findings as actionable and four as
intentional or precision false positives.

Recorded durations were 370.7 seconds for default, 99.0 seconds for
recommended, 130.2 seconds for strict, and 162.7 seconds for pedantic.
Cumulative allocations ranged from 7.79 GiB to 8.80 GiB; they are not peak
resident-memory measurements and do not establish a release budget.

## Findings

The actionable findings are:

- an impossible upper-bound comparison on a `uint32` series offset;
- a shadowed named error result whose deferred closure updates a different
  `err` binding;
- an unchecked query-log scanner terminal error; and
- three discarded output finalization errors from two tab writers and one gzip
  writer.

Two `nilness` findings deliberately index nil slices to exercise panic recovery.
Both source sites say the panic is intentional. Two `unreachable-code`
findings are final returns after `testing.T.Fatalf`; the general testing
no-return precision correction in `972cfcf` covers them.

## Comparator Coverage

Go vet reports 26 `Seek` signature findings from its name-only standard-method
heuristic. Prometheus's methods implement its documented sample iterator, not
`io.Seeker`; Glippy moved the equivalent rule to pedantic in `7840a0c`.

Staticcheck's additional output does not establish a missing default rule:

- SA5012 is an intentionally odd argument-count panic test with an explicit
  suppression;
- SA4006 is generated lexer output;
- SA4010 is intentional stress allocation with suppressions;
- SA2001 is an intentional synchronization barrier with a suppression;
- SA6002 is performance-only; and
- the remaining findings are migration or style policy.

## Decision

Retain the six actionable diagnostics and the existing precision corrections.
Do not promote the standard-method heuristic or add a repository-specific
exception. The exact corpus must rerun at the latest Glippy revision before
this adjudication contributes to the v0.6 exit evidence.
