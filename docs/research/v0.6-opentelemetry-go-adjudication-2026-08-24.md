# v0.6 OpenTelemetry-Go Adjudication, 2026-08-24

## Evidence

Pinned corpus run `32698988231` used Glippy revision
`7840a0c03cf0d58ca0994ebd52e8d5b934ceeabf` and OpenTelemetry-Go revision
`736a14fcdca28b8cf5237e6b9b166ec6ed832bf7`. All four profiles completed:

| Profile | Findings |
| --- | ---: |
| default | 0 |
| recommended | 0 |
| strict | 1 |
| pedantic | 4 |

There are no default or recommended findings to classify. The strict finding
is `too-many-lines`; pedantic adds three `mixed-receiver-type` findings. These
remain opt-in maintainability and style signals and do not affect the v0.6
default/recommended precision gate.

The run completed against a generated-code-bearing public library without a
Glippy crash, invalid machine output, unsupported source state, or incomplete
profile.

## Comparator Coverage

`go vet` completed without findings. Staticcheck reported three `U1000`
diagnostics for `nonComparable` function fields in test-only wrapper types.
Each field deliberately makes its containing struct non-comparable, and the
source documents that it is not called. The fields therefore have a structural
purpose despite having no direct read. They do not establish a missing default
Glippy rule.

## Decision

Record OpenTelemetry-Go as clean for the default and recommended profiles. It
adds no precision correction or default-rule candidate. Retain the strict and
pedantic findings as opt-in output for later profile-level usefulness review.

The complete exact corpus must rerun at the latest Glippy revision before this
adjudication contributes to the v0.6 exit evidence.
