# v0.6 Terraform Adjudication, 2026-08-24

## Evidence

Pinned corpus run `32698988231` used Glippy revision `7840a0c` and Terraform
revision `883c7221ee2e0bc2fad6912fd4b3cc4b53a598a7`. All referenced source
digests match the retained immutable source snapshot. All four profiles
completed:

| Profile | Findings |
| --- | ---: |
| default | 4 |
| recommended | 25 |
| strict | 821 |
| pedantic | 1,026 |

The run analyzed 1,868 owned files in 305 packages and loaded 2,065 files.
Manual review classifies 24 default or recommended findings as actionable and
one as a precision false positive.

Recorded durations were 405.9 seconds for default, 187.1 seconds for
recommended, 245.5 seconds for strict, and 306.6 seconds for pedantic.
Cumulative allocations ranged from 11.13 GiB to 12.62 GiB; they are not peak
resident-memory measurements and do not establish a release budget.

## Findings

The actionable findings are:

- nine stale, impossible, or tautological nilness conditions;
- eleven unchecked scanner terminal errors across interactive input, diagnostic
  rendering, authentication data, tests, and tooling;
- three discarded XML or ZIP finalization errors; and
- one provider cleanup deferred before its acquisition error is checked.

The sole `unreachable-code` finding is a final return after `testing.T.FailNow`.
The general testing no-return precision correction in `972cfcf` covers it.

## Comparator Coverage

Go vet and Staticcheck both completed without findings. This repository adds no
comparator-derived default-rule candidate.

## Decision

Retain the existing nilness, scanner, writer-finalization, and deferred-cleanup
rules at their current profiles. No Terraform-specific exception is justified.
The exact corpus must rerun at the latest Glippy revision before this
adjudication contributes to the v0.6 exit evidence.
