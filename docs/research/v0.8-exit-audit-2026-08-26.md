# v0.8 Exit Audit, 2026-08-26

## Decision

v0.8 large-repository hardening is complete at revision `3afa56c`.
Stable-v1 roadmap progress advances from 75% to 85%, and v0.9 stability freeze
and tagless release-candidate acceptance becomes active.

## Gate

| Requirement | Evidence | Result |
| --- | --- | --- |
| Exact pinned corpus | Run `32962070813` passed 17 repository audits, harness validation, matrix resolution, and combined collection | Complete |
| Default and recommended adjudication | All 334 findings are classified; default has zero false positives; no new fingerprint or rule candidate exists | Complete |
| Formatter source fidelity | All supported and buildable snapshots completed without internal, parse, equivalence, comment, directive, or idempotency failure | Complete within supported source/build scope |
| Transactional fix rehearsal | Safe-fix preview completed on supported snapshots without mutating external checkouts; cgo failures remain package-local | Complete within supported source/build scope |
| Modules, workspaces, tests, tags, cgo, generated code, and variants | The corpus exercises each boundary; unsupported versions, missing generated input, cgo coverage, and nested modules are explicit gaps | Complete with 11 documented boundaries |
| Native latency and memory | Run `32967515640` passed macOS/Linux amd64/arm64 editor, formatter, typed, and aggregate-memory ceilings | Complete |
| Native write/fix and release candidate | The same run passed native write/fix tests, frozen contracts, upgrade fixtures, tagless archives, and cross-runner reproducibility | Complete |
| Final fuzz | Run `32967508684` passed all nine bounded fuzz jobs | Complete |
| Plain CI and editor guidance | GitHub Actions, Woodpecker, shell CI, stdin/stdout, and existing-LSP examples are published without first-party plugins or Actions | Complete |
| Upgrade rehearsal | Historical Gox and early-Glippy configuration and baseline fixtures pass on every native candidate | Complete |

## Scope

Public repositories remain immutable validation inputs. No upstream adoption,
custom Action, editor plugin, package-manager integration, hosted service, or
community marketplace is part of this gate. Windows, network filesystems, and
forced-power-loss durability remain outside the supported product boundary.

The 19 report units called `unresolved` are incomplete executions already
covered by 11 required, ordered, non-actionable gaps. They do not represent
unreviewed diagnostics. Supported and buildable snapshots have complete
formatter, fixer, profile, and comparator evidence.

No tag or release was created.
