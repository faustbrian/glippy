# v0.8 Corpus Adjudication Summary, 2026-08-26

Pinned corpus run `32962070813`, attempt 1, passed all 20 jobs at Glippy
revision `3afa56cbf633f79b6811d9fc6bd4137256526822` with Go 1.27.0 and
Staticcheck 2026.2.1. The canonical adjudication and report are:

- [`v0.8-corpus-adjudication-32962070813.json`](v0.8-corpus-adjudication-32962070813.json)
- [`v0.8-corpus-report-32962070813.json`](v0.8-corpus-report-32962070813.json)

## Findings

All 334 default and recommended findings are classified:

| Profile | True positive | Intentional | False positive | Duplicate vet | Duplicate Staticcheck |
| --- | ---: | ---: | ---: | ---: | ---: |
| default | 66 | 1 | 0 | 1 | 15 |
| recommended | 229 | 6 | 0 | 1 | 15 |

Every current fingerprint existed in the previously reviewed exact-source
candidate. The corrected analysis removed 42 superseded entries: 40
Kubernetes testing-return or infallible-writer false positives, one
build-variant Containerd nilness false positive, and one NATS nilness false
positive. The remaining Containerd fuzz-tar diagnostic is accurate but
intentional in both profiles. Three NATS loop-postcondition assertions and two
Prometheus panic-recovery inputs are also accurate diagnostics on deliberately
accepted test source, so they are classified as intentional rather than false
positive. No default or recommended false positive remains and no new rule
candidate was produced.

## Formatter And Fixer

The formatter selected 29,657 files across the corpus and reported 24,899
aggregate canonical differences. Individual layout differences are adoption
volume, not diagnostics requiring manual adjudication. It completed on all
supported, buildable snapshots without an internal, parse, equivalence,
comment, directive, or idempotency failure. Four repository-wide formatter
units remain deliberately incomplete:

- CockroachDB contains a nested module selecting unsupported Go 1.23 source;
- Go-Ethereum selects unsupported Go 1.24 source;
- go-sqlite3 selects unsupported Go 1.21 source; and
- Moby contains a nested Go 1.24 module.

Transactional safe-fix preview completed on every otherwise supported
snapshot, including Moby after cgo failures were isolated to their owning
packages. CockroachDB analysis and fixing require generated `TEAMS.yaml`, while
its formatter retains the separate unsupported-Go-1.23 boundary. Go-Ethereum
and go-sqlite3 retain their unsupported-source boundaries. No external
checkout was modified.

## Accepted Boundaries

Eleven ordered `not-actionable` gaps cover 19 deliberately incomplete units:
the four formatter units above; unsupported analysis, comparator, and fixer
units for CockroachDB, Go-Ethereum, and go-sqlite3; and typed-analysis coverage
for Moby's two cgo packages. These are documented scope boundaries, not
unclassified findings. The report's `unresolved` count intentionally measures
incomplete execution units even when a required gap has an explicit
disposition; it must not be interpreted as 19 unreviewed diagnostics.

## Exit Decision

The exact corpus confirms the v0.7 precision corrections, SQLC comment
ownership, supported-source formatter fidelity, package-isolated fixing, and
large-repository runtime bound. Together with exact CI `32960773579`, final
fuzz `32967508684`, and native release-budget and reproducibility run
`32967515640`, this closes v0.8 at 85% stable-v1 roadmap progress. v0.9
stability freeze and tagless release-candidate acceptance are now active.
